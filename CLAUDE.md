# Project Instructions for Claude

## Git Workflow
- **Always work on the `develop` branch**, not `main`
- Merge to `main` only when explicitly requested
- Commit to `develop` first, then merge to `main` if needed

## Branch Structure
- `main` - production branch, deploy target
- `develop` - active development branch
